# ───── Data source (NO inline comments on the same line) ─────
spring.datasource.url=jdbc:mysql://localhost:3306/vit_results?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# ───── JPA / Hibernate ─────
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect  # optional – MySQL is auto-detected

server.port=8080
